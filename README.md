# extractinator
Bash script to make extracting compressed files a little easier
You can run it, source it and use it on the comand line, in another script, or add it to your .bashrc

chmod +x shell-extractinator.sh
./shell-extractinator.sh

source ./shell-extractinator.sh

Usage: extractinator <path/file.ext> [optional-extract-to-dir]
